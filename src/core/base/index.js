const basePagination = require('./pagination');
const baseValidation = require('./validation');
const baseSchema = require('./schema');

module.exports = {
  baseSchema,
  basePagination,
  baseValidation
};